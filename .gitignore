# Env / secrets
.env
.env.local
.env.*.local

# Node
node_modules/

# Python
venv/
__pycache__/
*.pyc

# Build cache — large local-only files generated by 01_build_pairs.py
# These are never committed; regenerated automatically on next run.
data/cache/

# Raw source CSVs (large, private data — not for version control)
raw/

# Vercel
.vercel/

# OS
.DS_Store
.vercel

# Editor / agent local settings
.claude/

# Local Netlify folder
.netlify
